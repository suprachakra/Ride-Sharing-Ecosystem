import UIKit

class BookingViewController: BaseViewController {
    override func viewDidLoad() {
        super.viewDidLoad()
        self.title = "Book a Ride"
        // Setup UI elements (buttons, text fields) for booking
        // TODO: Add booking functionality
    }
}
