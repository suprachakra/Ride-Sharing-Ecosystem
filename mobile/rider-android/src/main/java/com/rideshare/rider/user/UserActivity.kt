package com.rideshare.rider.user

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.rideshare.rider.R

/**
 * UserActivity manages the rider's account details.
 */
class UserActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main) // Change to appropriate layout if needed.
        // TODO: Implement user profile management (view/update profile, settings).
    }
}
