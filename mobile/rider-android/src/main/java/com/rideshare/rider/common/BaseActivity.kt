package com.rideshare.rider.common

import androidx.appcompat.app.AppCompatActivity

/**
 * BaseActivity provides common functionality (e.g., logging, error handling)
 * that can be extended by all activities.
 */
open class BaseActivity : AppCompatActivity() {
    // Common functions, such as showing a progress spinner or error dialogs, can be added here.
}
